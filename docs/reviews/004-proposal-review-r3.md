    18	    "spawnWeight": {
    19	      "normal": 5,
    20	      "elite": 1,
codex
READINESS: 76% | VERDICT: NO | FINDINGS:
[P1] Dead combatants can still act later in the same tick — Proposal §2 `_tickMonsterFight` — damage is applied, but death is only resolved after hero attack, hero skill, monster attack, and monster skill all run. A monster reduced to `<= 0` by the hero can still attack/skill in that same update, and a hero killed by the monster’s normal hit can still be hit again by the monster skill. Suggestion: resolve after each action, or short-circuit each later phase when either side is already dead.

[P1] Trap damage logic does not match the real data contract — Proposal §5 `_resolveTrap` vs [traps.json](/Users/admin/Game%20Jam%20創業/src/data/traps.json:1) — the proposal reads `trapDef.damage` and multiplies by raw `level`, but the actual schema is `baseDamage` plus `levels[].damageMultiplier`. This will produce systematically wrong battle results. Suggestion: look up the matching `levels[level-1]` entry and compute `Math.round(baseDamage * damageMultiplier)`.

[P1] Converted monster buffs are lost in battle and on restore — Proposal §§2/7 vs [GameState.js](/Users/admin/Game%20Jam%20創業/src/models/GameState.js:73) and [GameState.js](/Users/admin/Game%20Jam%20創業/src/models/GameState.js:94) — `createConvertedMonster()` stores persistent `buffFlags` (`hpMult`, `atkMult`), but the proposal’s combat path only reads `cell.monster.typeId/currentHp` and restores from raw `baseHp`. Converted monsters will fight as plain base monsters and reset as plain base monsters. Suggestion: resolve combat stats from roster instance by `instanceId`, or materialize effective HP/ATK onto the placed cell state when placing the monster.

[P2] Boss-kill timing is one frame late — Proposal §2 `update(dt)` / shared boss attack block — end conditions are checked before the shared boss attack. If the boss kills the last hero, `heroDefeated` processing and `battleEnd('win')` are deferred to the next frame, leaving a dead target in the queue briefly. Suggestion: run boss tick before final end-condition evaluation, or re-run end checks after the boss tick.

[P2] Hero special traits are silently dropped even though current data models them as combat-relevant — Proposal §§5/6 vs [heroes.json](/Users/admin/Game%20Jam%20創業/src/data/heroes.json:3) — at least the swordsman trap block, archer first-trap skip, priest undead bonus, and holy knight undead bonus materially affect battle outcomes. For MVP it is fine to defer them, but leaving them undocumented creates expectation mismatch. Suggestion: add an explicit “A1 intentionally ignores `specialTrait` effects” note, or implement the trap-related ones now because they directly affect core loop correctness.

[P3] `win` / `lose` result naming is semantically ambiguous — Proposal §2 `_endBattle(result)` — here `win` means “defense succeeded” and `lose` means “boss died”. That is internally consistent, but later UI or analytics code can easily read it from the hero/player perspective and invert meaning. Suggestion: rename to `defenseSuccess` / `bossBreached` or similarly explicit enums.
tokens used
36,538
READINESS: 76% | VERDICT: NO | FINDINGS:
[P1] Dead combatants can still act later in the same tick — Proposal §2 `_tickMonsterFight` — damage is applied, but death is only resolved after hero attack, hero skill, monster attack, and monster skill all run. A monster reduced to `<= 0` by the hero can still attack/skill in that same update, and a hero killed by the monster’s normal hit can still be hit again by the monster skill. Suggestion: resolve after each action, or short-circuit each later phase when either side is already dead.

[P1] Trap damage logic does not match the real data contract — Proposal §5 `_resolveTrap` vs [traps.json](/Users/admin/Game%20Jam%20創業/src/data/traps.json:1) — the proposal reads `trapDef.damage` and multiplies by raw `level`, but the actual schema is `baseDamage` plus `levels[].damageMultiplier`. This will produce systematically wrong battle results. Suggestion: look up the matching `levels[level-1]` entry and compute `Math.round(baseDamage * damageMultiplier)`.

[P1] Converted monster buffs are lost in battle and on restore — Proposal §§2/7 vs [GameState.js](/Users/admin/Game%20Jam%20創業/src/models/GameState.js:73) and [GameState.js](/Users/admin/Game%20Jam%20創業/src/models/GameState.js:94) — `createConvertedMonster()` stores persistent `buffFlags` (`hpMult`, `atkMult`), but the proposal’s combat path only reads `cell.monster.typeId/currentHp` and restores from raw `baseHp`. Converted monsters will fight as plain base monsters and reset as plain base monsters. Suggestion: resolve combat stats from roster instance by `instanceId`, or materialize effective HP/ATK onto the placed cell state when placing the monster.

[P2] Boss-kill timing is one frame late — Proposal §2 `update(dt)` / shared boss attack block — end conditions are checked before the shared boss attack. If the boss kills the last hero, `heroDefeated` processing and `battleEnd('win')` are deferred to the next frame, leaving a dead target in the queue briefly. Suggestion: run boss tick before final end-condition evaluation, or re-run end checks after the boss tick.

[P2] Hero special traits are silently dropped even though current data models them as combat-relevant — Proposal §§5/6 vs [heroes.json](/Users/admin/Game%20Jam%20創業/src/data/heroes.json:3) — at least the swordsman trap block, archer first-trap skip, priest undead bonus, and holy knight undead bonus materially affect battle outcomes. For MVP it is fine to defer them, but leaving them undocumented creates expectation mismatch. Suggestion: add an explicit “A1 intentionally ignores `specialTrait` effects” note, or implement the trap-related ones now because they directly affect core loop correctness.

[P3] `win` / `lose` result naming is semantically ambiguous — Proposal §2 `_endBattle(result)` — here `win` means “defense succeeded” and `lose` means “boss died”. That is internally consistent, but later UI or analytics code can easily read it from the hero/player perspective and invert meaning. Suggestion: rename to `defenseSuccess` / `bossBreached` or similarly explicit enums.
--- GEMINI R3 ---
Loaded cached credentials.
READINESS: 95% | VERDICT: YES | FINDINGS:
[P2] issue — Boss can be "overkilled" within a single frame — `_tickBossFight` vs main `update` loop — The check for `gameState.bossHp <= 0` happens at the end of the main `update` loop. If multiple heroes attack the boss in the same frame after its HP drops below zero, the battle continues until the loop finishes, potentially resulting in significant negative HP. This could affect scoring or subsequent logic. — Suggestion: After any damage is dealt to the boss in `_tickBossFight`, immediately check `if (gameState.bossHp <= 0)` and if so, call `_endBattle('lose')` and break or return from the update loop to prevent further actions in the same frame.
[P2] issue — Torture progress advances per hero defeated, which may be unintentionally fast — `_heroDefeated` — The current logic calls `advanceTortureProgress()` for every hero that is defeated. In a battle where 5 heroes die, each prisoner's progress advances by 5. The function name implies a single step. This could lead to unexpectedly rapid torture completions. — Suggestion: Clarify if this is the intended design. If not, consider moving the `advanceTortureProgress()` call to `_endBattle()` so it runs only once per battle, regardless of how many heroes were defeated.
[P3] issue — Documentation mismatch for waiting hero state — Risk section vs. `update` loop `[moving]` state logic — The Risk section states a waiting hero "state stays 'moving' with paused timer". However, the `update` loop logic correctly transitions the hero to a more explicit `'waitingForCombat'` state. The implementation is better, but the documentation is inconsistent. — Suggestion: Update the Risk section text to reflect the actual implementation (`'waitingForCombat'` state) for accuracy.
